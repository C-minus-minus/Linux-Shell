This program mimics a linux shell
It was for a project in school and probably makes zero sense

To compile run this:
	make clean
	make

To execute run:
	./output

You may also execute to run commands from a file:
	./output file

